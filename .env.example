# ============================
# OpenWebUI + Qdrant env config
# ============================

# OpenWebUI exposed port
OPENWEBUI_PORT=3000

# Qdrant service (Docker network)
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# Ollama runs on host (NOT in Docker)
# Mac / Windows:
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Linux users:
# OLLAMA_BASE_URL=http://localhost:11434
# (requires extra_hosts: host.docker.internal:host-gateway in docker-compose)
